# Intentionally public-facing ALB for the frontend
AVD-AWS-0053

# Unrestricted egress is common & intentional for these security groups
AVD-AWS-0104

# HTTP listener – will be upgraded to HTTPS + ACM later when domain is ready
AVD-AWS-0054

# EKS public endpoint – acceptable for this project stage
AVD-AWS-0040
AVD-AWS-0041

# S3 state bucket uses SSE-S3 (AES256) instead of CMK
AVD-AWS-0132